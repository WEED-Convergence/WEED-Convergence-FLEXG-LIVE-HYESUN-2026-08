# 시작하기 — CONVERGENCE Docs

이 저장소를 처음 받았다면 **이 페이지만** 보면 됩니다. (상세: [`docs/지침.md`](docs/지침.md))

## 1. 실행

```bash
npm install     # 최초 1회
npm run dev     # http://localhost:3000
```

| 열어볼 곳 | 무엇 |
| --- | --- |
| `/docs` | 기획 문서(좌 트리 · 중앙 프리뷰 · 우 설명) |
| `/docs/overview` | 이 프로토타입 개요(표지) |
| `/docs/pages` | 작업페이지 한눈에 보기(화면 목록 + 상태) |
| `/components` | 컴포넌트 표준(디자인 시스템) |

## 2. Claude Code로 시작 (권장)

경로·구조를 외울 필요 없습니다. **Claude Code를 열고**:

- `/start` 를 치면 → 가이드가 시작됩니다(환경 확인 → "무엇을 할지" → 서비스/페이지 질문 → 표준 컴포넌트로 조립).
- 또는 그냥 **"새 화면 만들래"** 처럼 하고 싶은 걸 말하면 Claude가 알아서 물어보고 시작합니다.

## 3. 기억할 3가지

1. **표준 컴포넌트로 조립** — `src/design-system`(배럴)에서 가져다 쓴다. 없으면 임시로 만들고 설명에 「디자인시스템 요청 대상」이라 남긴다. (직접 컴포넌트화 ✕)
2. **설명은 기능/예외 케이스로** — 컴포넌트명·색·px는 설명에 쓰지 않는다.
3. **작업은 브랜치 → PR** — `main` 직접 push 금지.

> 더 알고 싶으면 [`docs/지침.md`](docs/지침.md), AI 협업 규칙은 [`CLAUDE.md`](CLAUDE.md).
